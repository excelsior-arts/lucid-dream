"""Lucid Talk — a local voice companion.

Run it with `python -m lucid_talk`, or through run.sh. The code and everything
that ships with it live in this package; your data — sessions, memory, your
personas and prompts, your clips — lives in the checkout beside it, so an
edit to the code never disturbs it. See paths.py.
"""
